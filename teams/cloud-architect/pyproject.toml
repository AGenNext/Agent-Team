[project]
name = "cloud-architect-agent-team"
version = "0.1.0"
description = "Governed Cloud Architect Agent Team for AGenNext"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7.0",
  "pyyaml>=6.0.1"
]

[tool.setuptools.packages.find]
where = ["src"]
